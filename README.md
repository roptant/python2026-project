# python2026-project

Small Python project built around a dataset of the top 5 European football leagues.

## Contents

- `main.ipynb` for analysis and exploration
- `unprocessed/` for the raw CSV files and dataset metadata
- `processed/` for cleaned or derived outputs

## Data

The bundled data comes from the football-data.org API and includes leagues, teams, matches, scores, and standings.